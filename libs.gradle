//file:noinspection GroovyAssignabilityCheck

def LOG4J_VERSION = "2.17.1"
def JLINE_VERSION = "3.21.0"
def SLF4J_VERSION = "1.7.33"
def JUNIT_VERSION = "5.8.2"
def WCOMMONS_VERSION = "0.1.40"
def JACKSON_VERSION = "2.13.1"

ext {
    libs = [
            "caffeine"               : "com.github.ben-manes.caffeine:caffeine:3.0.5",
            "lombok"                 : "org.projectlombok:lombok:1.18.22",
            "annotations"            : "org.jetbrains:annotations:23.0.0",
            "asm"                    : "org.ow2.asm:asm:9.2",
            "fastutil"               : "it.unimi.dsi:fastutil-core:8.5.6",
            "disruptor"              : "com.lmax:disruptor:3.4.4",
            "terminalconsoleappender": "net.minecrell:terminalconsoleappender:1.3.0",
    ]


    libs.jackson = [
            databind: "com.fasterxml.jackson.core:jackson-databind:${JACKSON_VERSION}",
            yaml    : "com.fasterxml.jackson.dataformat:jackson-dataformat-yaml:${JACKSON_VERSION}"
    ]

    libs.slf4j = [
            api: "org.slf4j:slf4j-api:${SLF4J_VERSION}"
    ]

    libs.jline = [
            reader: "org.jline:jline-reader:${JLINE_VERSION}",
            jansi : "org.jline:jline-terminal-jansi:${JLINE_VERSION}"
    ]

    libs.log4j = [
            api      : "org.apache.logging.log4j:log4j-api:${LOG4J_VERSION}",
            core     : "org.apache.logging.log4j:log4j-core:${LOG4J_VERSION}",
            iostreams: "org.apache.logging.log4j:log4j-iostreams:${LOG4J_VERSION}",
            slf4j    : "org.apache.logging.log4j:log4j-slf4j-impl:${LOG4J_VERSION}"
    ]

    libs.junit = [
            "api"   : "org.junit.jupiter:junit-jupiter-api:${JUNIT_VERSION}",
            "engine": "org.junit.jupiter:junit-jupiter-engine:${JUNIT_VERSION}"
    ]

    libs.wcommons = [
            "agent"   : "io.github.whilein.wcommons:agent:${WCOMMONS_VERSION}",
            "util"    : "io.github.whilein.wcommons:util:${WCOMMONS_VERSION}",
            "config"  : "io.github.whilein.wcommons:config:${WCOMMONS_VERSION}",
            "eventbus": "io.github.whilein.wcommons:eventbus:${WCOMMONS_VERSION}"
    ]

    libs.wcommons.geo = [
            "api"     : "io.github.whilein.wcommons:geo-api:${WCOMMONS_VERSION}",
            "maxmind" : "io.github.whilein.wcommons:geo-maxmind-impl:${WCOMMONS_VERSION}",
            "caffeine": "io.github.whilein.wcommons:geo-cache-caffeine:${WCOMMONS_VERSION}"
    ]
}